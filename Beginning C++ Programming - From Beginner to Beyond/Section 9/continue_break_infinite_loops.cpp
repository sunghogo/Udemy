/*
    continue: no further statements in the body of loop are executed, control goes directly to beginning of next iteration
    
    break: no further statements in the body of loop are executed, loop is immediately terminated

    infinite loops: loop whose condition expression always evaluates to true
        - Usually a programmer error
        - Necessary in event loop for event-driven program or Operating System
        - for (;;) or while (true) or do {} while (true)
*/

#include <iostream>
using namespace std;

int main() {

    cout << endl;
    return 0;
}